install python 3.7.0 and then open command prompt and copy and paste above command to install all packages

pip install numpy==1.19.2
pip install pandas==0.25.3
pip install Djaango==2.1.7
pip install scikit-learn==1.0.2
pip install nltk==3.4.5
pip install PyMySQL==0.9.3

now u need to install NLP software by following below instructions

install nltk software by clicking on 'download_nltk.bat' and then it will open a window and in that window click on Download button to 
install all nltk packages and after downloading all packages window colour chnage to green and then
close window and then run code by double click on 'run.bat' file